# セキュリティレビュー結果

> このドキュメントは、本アプリ（Claude Code 講座の課題提出物）を Streamlit Community Cloud で
> 公開するにあたって実施したセキュリティレビューの記録です。
> レビュー観点: シークレット漏洩 / プロンプトインジェクション / 出力・XSS / 依存関係の脆弱性。
>
> 実施日: 2026-06-22

## 前提（リスク文脈）
- **対象**: AIライティングツール（Streamlit マルチページ + Gemini API / `google-genai`）
- **想定利用形態**: **インターネット公開**（Streamlit Community Cloud）。認証なしで誰でもURLに到達可能。APIキーは各利用者がサイドバーから入力する「自分のキーで使う」方式。
- **この前提の影響**: ローカル個人ツールなら軽微で済む項目が、公開＋匿名アクセスでは **コスト悪用・キー誤用** に直結し重大度が上がる。特に「アプリ所有者のキーが匿名利用者に使われる経路」の有無が最重要。

## サマリー（レビュー時点）
| 重大度 | 件数 |
|--------|------|
| Critical | 0 |
| High | 1（条件付き・確認の結果は未発生） |
| Medium | 2 |
| Low | 2 |
| Info | 1 |

すべての所見について対応済み（下表）。レビュー時点で実際に発生していた漏洩・事故はなし。

| # | 重大度 | 所見 | 対応状況 |
|---|--------|------|----------|
| ① | High（条件付き） | 環境変数フォールバックが公開時に「所有者キーの共有口」になりうる | ✅ 対応済み（Secrets未設定を確認＋コードでガード） |
| ② | Medium | キー入力欄の説明文が実態とずれている | ✅ 対応済み |
| ③ | Medium | `requirements.txt` のバージョン非固定 | ✅ 対応済み |
| ④ | Low | エラー表示が例外メッセージをそのまま画面に出す | ✅ 対応済み |
| ⑤ | Info | 認証なし公開（自分のキーで使う設計のため妥当） | ℹ️ 設計として許容 |

---

## 所見と対応

### ① [High（条件付き）] 環境変数フォールバックが、公開アプリでは「所有者キーの共有口」になりうる
- **場所**: `utils/gemini_client.py` の `get_api_key()`
- **観点**: シークレット漏洩 / 公開デプロイ
- **内容**: キー取得が「サイドバー入力 → 無ければ環境変数 `GEMINI_API_KEY`」の順だった。Streamlit Community Cloud は Secrets に設定した値を環境変数としても露出するため、Secrets に `GEMINI_API_KEY` を入れていると、キー未入力の匿名訪問者全員が所有者のキーで実行できてしまう。
- **影響**: 公開URL経由でのコスト悪用、レート枯渇、最悪はAPIキー／アカウントの停止。
- **対応**:
  1. Community Cloud の **Settings → Secrets を確認し、`GEMINI_API_KEY` が未設定であることを確認**（事故は発生していなかった）。
  2. コード側でも環境変数フォールバックを既定で無効化。`ALLOW_ENV_KEY_FALLBACK=1` を明示設定したローカル開発時のみ有効になるよう変更（`utils/gemini_client.py`）。これにより、将来 Secrets にキーを設定し直しても公開環境では共有されない。

### ② [Medium] キー入力欄の説明文が実態とずれている
- **場所**: `utils/ui.py` の `api_key_input()` の `help`
- **観点**: シークレット漏洩 / 透明性
- **内容**: 「キーはブラウザのセッション内にのみ保持され、サーバーには保存されません」と表示していたが、`st.session_state` はサーバー側メモリに保持される。公開アプリで他人にキー入力を求める以上、送信先を正確に伝える必要がある。
- **対応**: 「入力したキーは生成リクエストの実行にのみ使われ、アプリのサーバー側セッションに一時的に保持される（ファイル・DBには保存されず、セッション終了で破棄）」と実態に即した文言へ修正。共有端末では使用後にタブを閉じる旨も追記。

### ③ [Medium] `requirements.txt` のバージョンが固定されていない
- **場所**: `requirements.txt`
- **観点**: 依存関係の脆弱性
- **内容**: `streamlit` / `google-genai` / `python-dotenv` が非固定で、ビルドのたびに最新版が入り再現性がなく、破壊的変更や脆弱性混入のリスクがあった。
- **対応**: 動作確認済みバージョンに固定（`streamlit==1.58.0` / `google-genai==2.8.0` / `python-dotenv==1.2.2`）。公開前に `pip install pip-audit && pip-audit` で既知CVEを確認することを推奨（手元での実施を推奨事項として記録）。

### ④ [Low] エラー表示が例外メッセージをそのまま画面に出していた
- **場所**: 全機能ページの生成処理 `except` ブロック（8ファイル）
- **観点**: シークレット漏洩
- **内容**: `st.error(f"生成中にエラーが発生しました: {e}")` で例外文字列を画面表示しており、キー断片やリクエスト内容が表示される可能性があった。
- **対応**: 画面表示は汎用メッセージに変更し、詳細は `logging.exception()` でサーバーログにのみ出力するよう全ページを統一。

### ⑤ [Info] 認証なしの公開
- **観点**: 公開デプロイ
- **内容**: 認証ゲートはないが、コスト負担が利用者自身のキーに乗る設計のため公開アプリとして妥当（①対応が前提）。
- **補足**: 閲覧者を限定したい場合は Community Cloud の Sharing 設定や簡易パスワードゲートを追加する選択肢あり（必須ではない）。

---

## 良い点（セキュアに作られている箇所）
- **XSSに対して安全**: 全8ページとも生成結果を `st.markdown(result)`（`unsafe_allow_html` 未指定）と `st.text_area` で表示しており、HTMLエスケープが有効。`components.html` / `st.html` の使用もなし。
- **キーのハードコード無し・`.env` は `.gitignore` 済み**: 機械スキャンでもコード内の直書きキーは検出ゼロ。リポジトリにキーは含まれない。
- **キー入力欄が `type="password"`**: 肩越しの覗き見に配慮。
- **プロンプトインジェクションの実害が小さい設計**: ユーザー入力をプロンプトに埋め込むが、出力を `subprocess`／ファイルパス／SQL／ツール実行などの危険な経路に再利用していない（表示のみ）。
- **HTTPS・XSRF保護**: Community Cloud は HTTPS 配信で、`.streamlit/config.toml` を置いていないため XSRF 保護も既定で有効。

---

## 結論
公開アプリの致命傷となりうる XSS・キーのハードコード・危険な出力再利用は元々回避できており、
最重要だった「所有者キーの共有経路」（①）も Secrets 未設定の確認とコード側ガードで塞いだ。
上記対応により、課題の公開成果物として十分安全な水準と判断する。
