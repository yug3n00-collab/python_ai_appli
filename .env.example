# ローカル開発でサイドバー入力を省きたいとき用（任意）。
# 通常はアプリ起動後にサイドバーの入力欄へキーを入力すれば動きます。
GEMINI_API_KEY=your_api_key_here

# 上の GEMINI_API_KEY をフォールバックとして使う場合だけ 1 にする。
# 公開環境（Streamlit Community Cloud 等）では設定しないこと
# （所有者のキーが匿名利用者に共有される事故を防ぐため）。
ALLOW_ENV_KEY_FALLBACK=1
